<Window x:Class="Wail_Vincent_AventCalender.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Wail_Vincent_AventCalender"
        mc:Ignorable="d"
         Foreground="{DynamicResource Theme_TextBrush}">

    <Window.Resources>
        <!-- THEME BRUSHES -->
        <SolidColorBrush x:Key="Red_BackgroundBrush" Color="#B00000" />
        <SolidColorBrush x:Key="Red_TextBrush" Color="White" />
        <SolidColorBrush x:Key="Green_BackgroundBrush" Color="#006B00" />
        <SolidColorBrush x:Key="Green_TextBrush" Color="White" />
        <SolidColorBrush x:Key="Gold_BackgroundBrush" Color="#D4AF37" />
        <SolidColorBrush x:Key="Gold_TextBrush" Color="#3C2510" />

        <SolidColorBrush x:Key="Theme_BackgroundBrush" Color="#B00000" />
        <SolidColorBrush x:Key="Theme_TextBrush" Color="White" />

        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Foreground" Value="{DynamicResource Theme_TextBrush}"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Background" Value="#FFF"/>
            <Setter Property="Foreground" Value="#B00000"/>
            <Setter Property="Margin" Value="0,8,0,0"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Margin" Value="0,8,0,0"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="/image/noel.gif" Stretch="UniformToFill"/>
        </Grid.Background>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="64"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="64"/>
        </Grid.ColumnDefinitions>

        <!-- Header -->
        <TextBlock 
            Text="Calendrier de l'Avent" 
            FontSize="32" 
            FontWeight="Bold" 
            Grid.Row="0" Grid.Column="1" 
            HorizontalAlignment="Center" VerticalAlignment="Top" 
            Margin="0,24,0,8"/>
        <!-- User Personalization -->
        <StackPanel Grid.Row="1" Grid.ColumnSpan="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,8,0,8">
            <TextBlock x:Name="WelcomeTextBlock" FontSize="18"/>
            <TextBlock x:Name="GenderMessageTextBlock" Margin="16,0,0,0" />
        </StackPanel>

        <!-- AJOUT : Sélecteur couleur du fond + texte jours restants -->
        <StackPanel Grid.Row="1" Grid.ColumnSpan="3" Orientation="Vertical" HorizontalAlignment="Center" Margin="0,70,0,0">

            <TextBlock Text="Changer la couleur du fond :" FontWeight="Bold"/>

            <ComboBox x:Name="BackgroundColorSelector" Width="211"
                      SelectionChanged="BackgroundColorSelector_SelectionChanged">
                <ComboBoxItem Content="Rouge" Tag="#B00000"/>
                <ComboBoxItem Content="Vert" Tag="#006B00"/>
                <ComboBoxItem Content="Doré" Tag="#D4AF37"/>
                <ComboBoxItem Content="Bleu" Tag="#0033CC"/>
                <ComboBoxItem Content="Blanc" Tag="#FFFFFF"/>
            </ComboBox>
            <TextBlock x:Name="DaysBeforeChristmasText"
                       FontSize="18" FontWeight="Bold"
                       Text="Il reste 2 jours avant Noël." Height="38" RenderTransformOrigin="0.5,0.5" Width="223">
                <TextBlock.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform/>
                        <SkewTransform AngleY="3.288"/>
                        <RotateTransform Angle="-3.216"/>
                        <TranslateTransform Y="6.358"/>
                    </TransformGroup>
                </TextBlock.RenderTransform>
            </TextBlock>
        </StackPanel>
        <!-- FIN AJOUT -->

        <!-- Main Content -->
        <Border Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" Margin="32,65,32,20" Padding="32" CornerRadius="16" Background="#CCBFBFAF" VerticalAlignment="Top" Height="331">
            <StackPanel>

                <!-- Countdown Section -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock x:Name="DaysLeftTextBlock" FontSize="24" FontWeight="Bold" />
                    <TextBlock x:Name="CountdownTextBlock" FontSize="24" FontWeight="Bold" />
                </StackPanel>

                <!-- Message + image -->

            </StackPanel>
        </Border>
    </Grid>
</Window>
