<Window x:Class="Wail_Vincent_AventCalender.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Wail_Vincent_AventCalender"
        mc:Ignorable="d"
         Foreground="{DynamicResource Theme_TextBrush}">
    <Window.Resources>
        <!-- THEME BRUSHES -->
        <SolidColorBrush x:Key="Red_BackgroundBrush" Color="#B00000" />
        <SolidColorBrush x:Key="Red_TextBrush" Color="White" />
        <SolidColorBrush x:Key="Green_BackgroundBrush" Color="#006B00" />
        <SolidColorBrush x:Key="Green_TextBrush" Color="White" />
        <SolidColorBrush x:Key="Gold_BackgroundBrush" Color="#D4AF37" />
        <SolidColorBrush x:Key="Gold_TextBrush" Color="#3C2510" />

        <SolidColorBrush x:Key="Theme_BackgroundBrush" Color="#B00000" />
        <!-- Default: Red -->
        <SolidColorBrush x:Key="Theme_TextBrush" Color="White" />

        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Foreground" Value="{DynamicResource Theme_TextBrush}"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Background" Value="#FFF"/>
            <Setter Property="Foreground" Value="#B00000"/>
            <Setter Property="Margin" Value="0,8,0,0"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Margin" Value="0,8,0,0"/>
        </Style>
    </Window.Resources>

    <Grid Background="{DynamicResource Theme_BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="64"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="64"/>
        </Grid.ColumnDefinitions>

        <!-- Header: Advent Calendar Title -->
        <TextBlock 
            Text="Calendrier de l'Avent" 
            FontSize="32" 
            FontWeight="Bold" 
            Grid.Row="0" Grid.Column="1" 
            HorizontalAlignment="Center" VerticalAlignment="Top" 
            Margin="0,24,0,8"/>

        <!-- Theme Selector -->
        <StackPanel Grid.Column="2" Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,24,16,0">
            <TextBlock Text="Thème : " VerticalAlignment="Center" Margin="0,0,8,0"/>
            <ComboBox x:Name="ThemeSelector" Width="96">
                <ComboBoxItem Content="Rouge" IsSelected="True" />
                <ComboBoxItem Content="Vert" />
                <ComboBoxItem Content="Doré" />
            </ComboBox>
        </StackPanel>

        <!-- User Personalization -->
        <StackPanel Grid.Row="1" Grid.ColumnSpan="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,8,0,8">
            <TextBlock x:Name="WelcomeTextBlock" FontSize="18"/>
            <Button Content="Entrer mon nom" x:Name="BtnEnterName" Margin="16,0"/>
            <Button Content="Choisir genre" x:Name="BtnChooseGender" Margin="16,0"/>
            <TextBlock x:Name="GenderMessageTextBlock" Margin="16,0,0,0" />
        </StackPanel>

        <!-- Main Content: Calendar Body -->
        <Border Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" Margin="32,0,32,32" Padding="32" CornerRadius="16" Background="#CCFFFFFF" VerticalAlignment="Top">
            <StackPanel>
                <!-- Countdown Section -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock x:Name="DaysLeftTextBlock" FontSize="24" FontWeight="Bold" />
                    <TextBlock Text=" | " FontSize="24" Margin="8,0"/>
                    <TextBlock x:Name="CountdownTextBlock" FontSize="24" FontWeight="Bold" />
                </StackPanel>

                <!-- Message/Image of the day -->
                <StackPanel Orientation="Vertical" HorizontalAlignment="Center" Margin="0,32,0,0">
                    <TextBlock Text="Message du jour :" FontWeight="SemiBold" FontSize="18" Margin="0,0,0,8"/>
                    <TextBlock x:Name="MessageTextBlock" FontSize="22" TextWrapping="Wrap" TextAlignment="Center" />
                    <Image x:Name="DailyImage" Width="128" Height="128" Margin="0,24,0,0" Stretch="Uniform"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Window>